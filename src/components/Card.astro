---
interface Props {
  title: string;
}

const { title }: Props = Astro.props;
---

<section
  class="flex flex-col items-center gap-4 rounded-xl border-4 border-slate-200 p-4"
>
  <h2 class="text-center text-xl font-semibold">{title}</h2>
  <div><slot /></div>
</section>
